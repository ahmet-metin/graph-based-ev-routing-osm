# Results

This folder contains selected outputs from the final multi-city benchmark used in the manuscript.

The main folder is `final_multicity_benchmark/`. City-specific outputs are stored under `final_multicity_benchmark/cities/`, and energy-consumption sensitivity outputs are stored under `final_multicity_benchmark/energy_per_km_sensitivity/`.
